devices:
  reg-rtr:
    hostname: 10.10.10.2
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: router

  ham-rtr:
    hostname: 10.10.10.3
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: router

  mid-rtr:
    hostname: 10.10.10.4
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: router

