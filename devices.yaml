devices:
  mgmt-rtr:
    host: 10.10.10.1
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: router

  reg-rtr:
    host: 10.10.10.2
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: router

  ham-rtr:
    host: 10.10.10.3
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: router

  mid-rtr:
    host: 10.10.10.4
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: router

  mgmt-sw:
    host: 10.10.10.5
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: switch

  ham-sw:
    host: 10.10.10.6
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: switch

  mid-sw:
    host: 10.10.10.7
    username: ${CISCO_USER}
    password: ${CISCO_PASS}
    secret: ${CISCO_PASS}
    type: switch
